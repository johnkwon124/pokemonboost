# Google Cloud Vision API key — used for OCR directly from the browser.
# Restrict it in the Google Cloud console to: Cloud Vision API only + HTTP referrers (your domain + localhost).
NEXT_PUBLIC_GOOGLE_VISION_API_KEY=

# Optional: Pokemon TCG API key for a higher rate limit. Not required.
NEXT_PUBLIC_POKEMONTCG_API_KEY=
